import styles from "./SelectUser.module.css";

function SelectUser()
{
    return (
        <div className={`${styles.container}`}>
            Select User Works!
        </div>
    );
}

export default SelectUser;